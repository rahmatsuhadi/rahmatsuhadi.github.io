var typed = new Typed('#typed', {
    // strings: ['','Web Developer'],
    strings: ['<i>Frontend Web Developer</i>.', 'Student at University'],
    typeSpeed: 100,
    loop:true,
    fadeOut:true,
    showCursor:false,
})



function scrollToSection() {
    // Menggunakan smooth scrolling untuk mengarahkan ke elemen tertentu
    document.querySelector('#form').scrollIntoView({ behavior: 'smooth' });
}


function scrollToSection() {
    // Menggunakan smooth scrolling untuk mengarahkan ke elemen tertentu
    document.querySelector('#form').scrollIntoView({ behavior: 'smooth' });
}